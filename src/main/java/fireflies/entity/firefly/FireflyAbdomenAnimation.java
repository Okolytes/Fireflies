package fireflies.entity.firefly;

public enum FireflyAbdomenAnimation {
    OFF, // constantly off
    ON, // constantly on
    CALM, // slow random flashing, not synchronized
    PULSE, // slow synchronized pulsing
    STARRY_NIGHT, // fast random flashing, not synchronized
    STARRY_NIGHT_SYNCHRONIZED // fast random flashing, synchronized
}
